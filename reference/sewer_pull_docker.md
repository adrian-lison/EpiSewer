# Pull the latest EpiSewer docker image

This function pulls the latest EpiSewer docker image from the github
container registry. This is necessary to use the docker backend for
model fitting.

## Usage

``` r
sewer_pull_docker()
```
