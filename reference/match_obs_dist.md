# Check the supplied observation distribution type and return the internal id

Check the supplied observation distribution type and return the internal
id

## Usage

``` r
match_obs_dist(distribution)
```
