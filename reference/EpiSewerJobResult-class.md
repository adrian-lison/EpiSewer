# The EpiSewerJobResult S4 class

An S4 class registration for `EpiSewerJobResult` objects. Instances are
S3 objects created by
[`EpiSewerJobResult()`](https://adrian-lison.github.io/EpiSewer/reference/EpiSewerJobResult.md).
The class registration enables `is(x, "EpiSewerJobResult")` checks.
