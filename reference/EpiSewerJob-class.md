# The EpiSewerJob S4 class

An S4 class registration for `EpiSewerJob` objects. Instances are S3
objects created by
[`EpiSewerJob()`](https://adrian-lison.github.io/EpiSewer/reference/EpiSewerJob.md).
The class registration enables `is(x, "EpiSewerJob")` checks.
