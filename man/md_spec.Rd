% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_mdaccess.R
\name{md_spec}
\alias{md_spec}
\title{Read a component argument from the spec tree}
\usage{
md_spec(md, slot, name, default)
}
\arguments{
\item{md}{Modeldata environment.}

\item{slot}{The component slot to read from (e.g. \code{"noise"}).}

\item{name}{The argument name. If missing, the whole slot spec (including
\verb{$.helper}, the helper function name) is returned.}

\item{default}{Value returned when the slot or argument is absent (also
when the chosen helper for that slot does not have the argument). If
missing, absence is an error.}
}
\description{
The spec tree (\code{md$.spec}) holds the captured arguments of
every model component, published before any component body runs. This is
the supported way for one component (or derivation) to read facts rooted
in another component's arguments.
}
\keyword{internal}
