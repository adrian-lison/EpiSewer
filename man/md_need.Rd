% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_mdaccess.R
\name{md_need}
\alias{md_need}
\title{Get a modeldata variable, deriving it on demand if necessary}
\usage{
md_need(md, path)
}
\arguments{
\item{md}{Modeldata environment.}

\item{path}{A \code{$}-separated variable path, e.g. \code{"se"} or
\code{".metainfo$length_I"}.}
}
\description{
\code{md_need()} is the single resolution mechanism for
cross-cutting derived variables: if the variable is already present in
the modeldata it is returned; otherwise its registered derivation (see
\code{\link[=modeldata_derivations]{modeldata_derivations()}}) is resolved recursively, the results are
memoized into the modeldata, and the value is returned. Cycles are
detected and reported with the full derivation chain.
}
\keyword{internal}
