% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_modelcomponent.R
\name{model_component}
\alias{model_component}
\title{Create a model component object}
\usage{
model_component(
  name,
  slot,
  .e,
  args = as.list(parent.frame()),
  requires_data = character(0),
  requires_assumptions = character(0)
)
}
\arguments{
\item{name}{Name of the public helper function this component was created
by (e.g. \code{"noise_estimate_dPCR"}). Used for error attribution and the
model structure summary.}

\item{slot}{The component slot this component fills. Must be one of
\code{\link[=all_components]{all_components()}}.}

\item{.e}{An expression that defines the component through changes to the
\code{modeldata} object. Must evaluate to (or explicitly return) the updated
\code{modeldata}. The expression can use all captured arguments by name.}

\item{args}{Arguments captured for the component. By default, all variables
in the calling function's frame (i.e. its arguments and any local
variables computed before the \code{model_component()} call). These are
published to the spec tree (\code{modeldata$.spec[[slot]]}) before any
component body runs.}

\item{requires_data}{Character vector of data inputs the component needs
(checked in the pre-flight pass). Alternatives can be expressed with
\code{"a|b"}. Can also be a \verb{function(spec)} returning such a vector, for
requirements that depend on the model specification.}

\item{requires_assumptions}{Like \code{requires_data}, for assumptions.}
}
\value{
An object of class \code{model_component}.
}
\description{
A \code{model_component} bundles the specification of one model
component: the public helper name, the component slot it fills, the
(normalized) arguments captured from the calling helper function, the
user inputs it requires, and a function that applies the component to a
modeldata object. Components are inert until executed by
\code{\link[=modeldata_compile]{modeldata_compile()}}, which runs them in a fixed slot order.
}
\keyword{internal}
